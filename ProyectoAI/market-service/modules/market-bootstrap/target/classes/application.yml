spring:
  application:
    name: alimentozero-market-service
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher

server:
  port: 8092

management:
  endpoints:
    web:
      exposure:
        include: health,info
